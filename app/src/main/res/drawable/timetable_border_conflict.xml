<?xml version="1.0" encoding="utf-8"?>
<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" >
    <stroke
        android:width="1dp"
        android:color="@color/conflict" />
    <gradient
        android:centerColor="@color/conflict"
        android:endColor="@color/conflict"
        android:startColor="@color/conflict"
        android:type="sweep" />
</shape>
